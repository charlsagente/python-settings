# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

orbs:
  python: circleci/python@2.1.1


workflows:
    example-workflow:
      jobs:
        - python/test:
            app-dir: ~/python-settings 
            ensure-test-tool: true 
            pypi-cache: true 
            test-tool: pytest 